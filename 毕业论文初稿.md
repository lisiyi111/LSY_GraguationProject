# 基于编码彩灯的上位机APP设计

姓名：李思懿  
院系：电子信息与通信学院  
专业：通信工程  
指导教师：龚军

## 摘要

随着舞台灯光、景观照明、艺术装置以及互动展示系统的快速发展，面向异形灯阵的数字化控制需求不断提高。传统彩灯控制方式多以人工参数配置、简单文本记录或下位机侧直接调试为主，存在参数量大、操作效率低、配置过程不直观、场景组织困难以及联调成本高等问题。针对多节点编码彩灯阵列在工程应用中的配置复杂、场景切换频繁、协议格式严格等特点，本文围绕“基于编码彩灯的上位机APP设计”展开研究，设计并实现了一套基于Unity与C#的三维可视化上位机软件。

本文研究对象为由356个彩灯构成的球形阵列系统。根据实际结构需求，系统将彩灯划分为31个控制组，其中包括20个12灯组、5个11灯组、5个9灯组以及1个16灯组，并按照“下边界和右边界归属本组”的原则完成平面化映射与编号设计。为了提升用户对异形结构的理解效率，本文采用Unity构建三维球体彩灯模型，并通过鼠标拖拽旋转、面选择、灯具点选、高亮显示和参数面板联动等方式，实现了对彩灯参数的直观化编辑。在单灯控制层面，系统将每个彩灯抽象为具备组号、组内编号、外圈RGBW参数、内圈RGBW参数及摄像头状态的对象，既满足可视化显示需求，又兼顾协议编码所需的数据完整性。

在数据组织与输出方面，本文依据下位机通信协议设计了场景数据封装方案。系统以场景为组织单位，对31组彩灯数据进行统一管理，并采用二进制文件形式输出控制内容。每组场景数据均包含头部标志位、组别及摄像头状态、彩灯RGBW占空比数据、校验和以及结束位，从而保证生成的`.bin`文件与下位机解析逻辑保持一致。为满足实际使用中多场景编排、插入、重排与覆盖更新的需要，本文进一步设计了场景缓存、场景插入、场景合并、场景加载、场景回写以及下拉联动编辑机制，使上位机既能够作为单次参数配置工具，又能够作为完整的场景管理平台。

在通信交互方面，系统实现了串口参数配置、端口扫描、文件发送、运行命令下发、清除命令发送以及线路检查命令发送等功能。针对线路检查反馈，系统在主线程中对接收缓冲区进行解析，识别`F2`帧结构并生成各组彩灯的状态结果，为用户提供组级故障定位信息。针对运行参数配置，系统按照协议将循环间隔、场景数和循环次数拆分为多个字节并封装为`F8`命令帧，支持场景文件下发后的联动运行。针对清空和检测操作，系统分别实现了`F6`和`F7`指令链路，形成了从参数生成、文件组织、串口传输到反馈显示的完整上位机工作流程。

结合当前项目实现结果，本文从系统需求分析、总体架构设计、关键技术研究、模块化实现及功能测试等方面，对基于编码彩灯的上位机APP进行了系统阐述。研究结果表明，本文设计的软件能够较好地完成异形彩灯阵列的三维可视化建模、参数编辑、多场景管理、二进制文件生成以及串口通信控制等任务，具有较强的可操作性和工程应用价值。该系统不仅可服务于当前球形编码彩灯控制场景，也可为景观照明、装置艺术、舞台控制等领域的上位机软件开发提供参考。

关键词：编码彩灯；上位机；Unity；C#；串口通信；场景管理；二进制文件

## Abstract

With the rapid development of stage lighting, landscape illumination, artistic installations and interactive display systems, the demand for digital control of irregular lamp arrays is increasing. Traditional control methods mainly rely on manual parameter configuration, plain-text records or direct debugging on lower controllers, which often lead to low efficiency, poor visualization, difficult scene management and high commissioning cost. Focusing on the practical needs of a multi-node coded lighting array, this paper designs and implements an upper-computer application based on Unity and C#.

The target system contains 356 lamps arranged on a spherical structure. According to the hardware layout, all lamps are divided into 31 groups, including twenty 12-lamp groups, five 11-lamp groups, five 9-lamp groups and one 16-lamp group. A three-dimensional visual model is built in Unity, and interactive operations such as model rotation, face selection, lamp highlighting and parameter panel synchronization are implemented to improve editing efficiency. Each lamp is abstracted as an object with group ID, lamp index, RGBW parameters for two rings and camera status, which supports both real-time visualization and protocol-oriented data encoding.

Based on the communication protocol of the lower controller, this paper further designs a binary scene packaging method. Every scene contains 31 groups of lighting data, and each group is encoded into a structured frame including header, group and camera state, RGBW duty-cycle data, checksum and end marker. The software also supports scene insertion, reordering, merging, loading and dropdown-based editing, so that it can work as both a parameter editor and a scene management platform.

In addition, the software provides serial-port configuration, command sending, file transmission, clear command, run command and line-check command. The received `F2` feedback frames are parsed in the main thread to identify abnormal lamps and display diagnostic information. The implemented system forms a complete workflow covering scene editing, binary generation, serial transmission and feedback visualization. The results show that the proposed upper-computer software can effectively support the configuration and control of coded spherical lamp arrays and has good practical value for related lighting applications.

Key words: coded lighting; upper computer; Unity; C#; serial communication; scene management; binary file

## 第一章 绪论

### 1.1 研究背景

近年来，随着LED器件性能提升、嵌入式控制技术发展以及数字媒体艺术的普及，大规模彩灯控制系统已广泛应用于舞台演出、城市景观、主题展陈、节庆亮化和互动艺术装置等场景。这类系统通常包含大量分布式光源节点，既要求对颜色、亮度和工作节奏进行精确控制，又要求支持快速切换不同显示效果。对于规则平面灯阵而言，控制软件往往只需提供二维参数表格或简单图元映射即可完成编辑；而对于球体、多面体、异形装置等空间结构，用户难以通过单纯数字表格建立空间感知，导致参数配置过程繁琐且容易出错。

在本课题所对应的工程背景中，彩灯系统采用多节点分层控制方式，由PC端或上位机统一生成控制文件，经通信链路传递至主下位机后，再由主下位机向各从机节点分发具体控制内容。由于下位机需按固定协议解析数据，若上位机无法准确完成组别划分、灯号映射、参数编码与文件组织，则会直接影响整个系统的显示效果和运行可靠性。因此，开发一套兼顾三维可视化、协议匹配和场景管理的上位机软件，具有明显的现实需求。

当前很多控制系统的软件设计仍停留在“命令发送工具”或“数据配置工具”的层面，难以同时满足异形结构展示、单灯级交互、场景编排、文件生成和联机控制等复合需求。而Unity引擎在三维可视化、对象管理、交互逻辑和跨平台部署方面具有天然优势，将其引入彩灯上位机开发，可以将抽象的数据配置过程转化为直观的空间操作过程，提高软件的可用性和工程表现力。

### 1.2 研究目的与意义

本课题的研究目的在于设计并实现一套面向编码彩灯阵列的上位机APP，使用户能够围绕实际球体结构完成彩灯参数编辑、分组管理、多场景组织、二进制文件生成以及串口联调控制。与传统参数配置方式相比，本系统强调“结构可视化、数据对象化、场景文件化、通信标准化”四个核心方向。

从工程应用意义来看，本文设计的软件能够显著降低大规模异形彩灯系统的配置门槛。通过三维模型和面级切换机制，用户可以在接近真实物理结构的环境中编辑参数；通过场景列表和二进制封装机制，用户可以快速生成符合下位机要求的控制文件；通过串口调试和线路检查反馈，用户可以在联调阶段快速定位通信状态与灯具异常问题。上述功能有助于缩短开发与调试周期，提高系统的可靠性与可维护性。

从专业培养意义来看，本课题涉及上位机软件设计、串口通信、协议封装、数据结构组织、三维可视化交互、人机界面联动以及测试验证等多个知识模块，具有明显的综合实践特征。通过本课题研究，可以深化对嵌入式系统与上位机协同工作的理解，锻炼以工程目标为导向进行软件系统设计与实现的能力。

从推广价值来看，本文虽然面向特定球形编码彩灯系统展开研究，但其总体思路同样适用于景观灯光、数字展陈、环境监测、能源显示及工业控制等需要“图形化配置+协议输出+设备联调”的场景。特别是基于Unity的三维交互方式，为未来面向数字孪生方向的设备可视化控制软件提供了借鉴。

### 1.3 国内外研究现状

国内关于灯光控制系统的研究，早期主要集中在下位机硬件控制、调光驱动与总线协议实现方面。相关研究围绕STM32、ARM、DMX512、PWM调光以及嵌入式灯具网络展开，重点解决彩灯驱动控制、混色算法与通信可靠性问题。这类研究为灯光系统控制提供了坚实的底层基础，但在上位机交互体验和异形结构映射方面相对不足。

随着可视化需求增加，国内研究开始关注图形界面与三维交互手段。开题报告中引用的研究表明，Unity3D已逐渐被引入物联网远程控制系统、智能环境监控和可视化交互软件的设计之中，三维模型与实时数据的结合成为提升用户操作体验的重要方向。这为本文采用Unity搭建球体彩灯阵列的三维交互界面提供了理论支持。

用户提供的参考文献《基于C#的土壤渗透系数测量系统上位机软件设计》虽然研究对象并非彩灯系统，但其在C#上位机开发、界面设计、参数采集、状态显示及数据管理等方面体现出明显的工程参考价值。该类研究说明，针对专用设备构建面向任务的上位机软件，核心在于围绕硬件特点设计适配性的交互流程和数据组织方式，而不是简单地堆叠通用控件。本文在系统设计中同样采用了这种“面向设备对象建模、面向协议生成数据、面向操作流程组织界面”的思路。

另一篇参考文献《光伏发电系统上位机软件设计与测试》从上位机软件的功能设计、测试流程及稳定性验证角度提供了借鉴。虽然其服务对象为光伏发电监测系统，但在模块划分、通信连接、数据展示与软件测试组织方面，与本文所研究的彩灯上位机软件存在较强共性。尤其是在“先完成功能分层，再通过测试验证系统正确性”的思路上，对本文第五章和第六章的结构安排具有参考意义。

国外研究则更多关注多协议兼容、网络化控制和跨平台系统集成。例如基于DMX512、Art-Net、USB串口及网络协议的复合照明控制系统已得到较多关注。部分研究强调控制固件的统一设计，部分研究强调可视化软件的远程协同能力。总体来看，国外在通信接口整合、标准协议兼容与网络化管理方面起步较早，但针对某一具体异形灯阵结构提供专用上位机方案的公开研究并不多见。

综合现有研究可以发现：其一，底层控制与总线通信方面的研究已较为成熟；其二，面向具体设备结构的专用上位机设计仍存在较大空间；其三，三维引擎逐渐成为复杂设备可视化交互的重要开发平台。基于上述背景，本文将三维可视化界面、彩灯对象化管理、场景二进制封装和串口联调控制结合起来，形成面向编码彩灯阵列的完整上位机软件方案。

### 1.4 研究内容

围绕“基于编码彩灯的上位机APP设计”这一主题，本文主要完成以下研究内容：

1. 分析球形编码彩灯阵列的结构特征，完成356个彩灯的分组、编号与控制对象抽象。
2. 基于Unity引擎构建三维彩灯场景，实现模型旋转、面选择、灯具点选、高亮和参数联动编辑。
3. 基于C#设计彩灯对象、场景对象和组数据结构，实现31组彩灯的统一建模与场景缓存。
4. 按照下位机协议要求，设计场景数据的二进制封装格式，实现多场景`.bin`文件生成与解析。
5. 实现串口模式下的端口配置、文件发送、运行命令、清空命令和线路检查命令，并对反馈帧进行解析。
6. 通过功能测试与流程验证，分析软件设计的正确性、稳定性和工程适用性。

### 1.5 论文结构安排

本文共分为六章。

第一章为绪论，介绍课题背景、研究意义、国内外研究现状、研究内容及论文结构。  
第二章为系统需求分析与总体方案设计，分析硬件背景、软件功能需求和总体架构。  
第三章为关键技术研究，重点讨论Unity三维交互、彩灯分组编号、协议编码与数据组织方法。  
第四章为系统详细设计与实现，围绕彩灯对象管理、场景编辑、文件读写和串口通信进行模块化说明。  
第五章为系统测试与结果分析，从功能测试、协议验证与交互效果等方面展开。  
第六章为总结与展望，对本文工作进行归纳，并提出后续改进方向。

## 第二章 系统需求分析与总体方案设计

### 2.1 系统应用背景

本系统服务于一个球体编码彩灯控制场景。整个物理系统由PC端、通信链路、主下位机、从机节点以及彩灯阵列构成。PC端通过上位机APP完成场景数据配置和协议命令下发；主下位机接收上位机指令后进行存储、转发或状态回传；从机节点根据接收到的数据控制相应彩灯工作。由于彩灯阵列并非规则平面分布，而是附着于球体或多面体外形上，因此需要将三维结构映射为易于理解和操作的软件界面。

根据设计图文档，本次设计共涉及356个彩灯，划分为31个控制组。各组灯数并不统一，存在9灯组、11灯组、12灯组和16灯组四种类型。对下位机而言，数据传输以组为单位进行；对用户而言，编辑过程则既需要看到整体球体结构，也需要在局部层面精准定位具体灯具。因此，上位机软件必须同时处理空间可视化和协议化输出两个维度的问题。

### 2.2 功能需求分析

结合开题报告、设计图以及当前项目实现，系统需要满足如下功能需求。

#### 2.2.1 三维展示与观察需求

系统应能够在启动后显示完整的球体彩灯模型，并支持用户通过鼠标拖拽对模型进行水平与垂直方向的观察调整。用户需要通过旋转操作查看不同区域的彩灯分布，必要时还应支持快速恢复默认视角，以降低长时间编辑带来的观察负担。

#### 2.2.2 面选择与局部编辑需求

由于356个彩灯同时显示会增加交互复杂度，系统需要支持按“面”进行分组查看。用户选中某一面后，仅显示该面的彩灯对象，从而提升局部编辑效率。该功能要求软件同时完成视角旋转与灯具显隐联动。

#### 2.2.3 单灯参数配置需求

系统应支持对单个彩灯进行点选，并在参数面板中显示该灯具的RGBW信息。根据实际控制要求，每个彩灯需包含两组RGBW参数：一组用于外部可视化展示并直接驱动灯光显示，另一组作为内部编码数据参与协议输出。对于带摄像头的特殊灯具，还需支持摄像头开关状态配置。

#### 2.2.4 场景管理需求

彩灯控制通常不是单一状态控制，而是多个显示场景依序播放。因此，系统应支持将当前所有彩灯参数保存为一个场景，并实现多个场景的顺序组织。用户需能够完成场景新增、指定位置插入、下拉切换应用、场景覆盖编辑、BIN文件导入、场景合并及顺序管理等操作。

#### 2.2.5 文件生成与加载需求

系统应能根据下位机协议要求，将多个场景组织为单一`.bin`文件输出，并支持从已有`.bin`文件中重新加载场景数据。文件生成不仅要保证字节顺序、校验方式和结束位设置正确，还要与31组不同灯数结构相匹配。

#### 2.2.6 串口联调与命令控制需求

系统应支持串口端口扫描、波特率设置、串口开关、BIN文件选择与发送，并支持运行命令、清空命令和线路检查命令的发送。为便于用户调试，软件还应显示发送与接收日志，并对部分反馈内容进行自动解析。

#### 2.2.7 故障反馈解析需求

线路检查时，下位机返回以`F2`开头的反馈帧。上位机需要根据组号和状态位组合判断某组内各个从机彩灯是否正常，并在日志中输出故障信息，为硬件联调提供依据。

### 2.3 非功能需求分析

除功能需求外，系统还需满足以下非功能需求。

1. 交互直观性。界面布局应尽量与用户工作流程一致，减少跳转和重复操作。  
2. 数据一致性。界面显示状态、当前灯具状态、场景缓存状态和输出文件内容应保持一致。  
3. 可维护性。系统应采用模块化脚本结构，便于后期增加协议命令或扩展灯具属性。  
4. 实时性。对灯具参数的编辑应及时在界面中反映，串口接收数据应在主线程中稳定解析。  
5. 兼容性。软件应适应Windows环境下的Unity部署，支持常见串口设备接入。  
6. 可扩展性。软件结构应支持未来增加更多组数、更多控制参数或更多通信方式。

### 2.4 系统总体方案

根据需求分析，本文将系统总体方案划分为用户交互层、业务控制层、数据模型层、文件输出层和通信交互层五个部分。

用户交互层负责三维场景显示、面板展示、输入框、滑块、下拉框和日志区域等界面元素，是用户与系统直接交互的入口。  
业务控制层负责处理用户操作逻辑，包括面切换、灯具选中、场景管理、模式切换、面板显隐以及流程控制。  
数据模型层负责抽象彩灯对象、场景对象和灯具数据结构，为参数编辑和文件封装提供统一的数据基础。  
文件输出层负责将场景数据转换为符合协议要求的二进制内容，并支持相反方向的解析加载。  
通信交互层负责串口连接、命令发送、数据接收、缓存处理和反馈解析。

这种分层方案与用户提供的两篇上位机参考文献中的模块化设计思想是一致的。无论是专用测量系统还是光伏监测系统，上位机软件都需要在界面、业务、数据和通信之间建立清晰边界。本文在此基础上进一步强化了三维交互与异形结构映射能力，使总体架构更加贴合彩灯控制这一应用场景。

### 2.5 业务流程设计

系统业务流程可概括如下：首先，用户启动程序并进入编辑模式，观察球体彩灯模型；其次，用户通过面选择功能切换到目标区域，对灯具进行点选和参数编辑；再次，用户将当前状态保存为场景，或者插入到已有场景列表中；随后，系统依据场景顺序生成`.bin`文件，或在串口模式下选择文件并向下位机发送；最后，用户可下发运行命令、清除命令或线路检查命令，并通过日志查看反馈结果。

在此流程中，系统特别重视“当前灯具编辑状态”和“场景列表状态”的一致性问题。为防止用户尚未将当前修改写回场景列表就直接生成文件，系统加入了提交提示与确认机制。该设计能有效减少误操作，是软件可靠性设计的重要体现。

## 第三章 关键技术研究

### 3.1 基于Unity的三维可视化交互技术

Unity引擎原本广泛应用于游戏和实时三维可视化领域，但其对象管理、碰撞检测、材质渲染、摄像机控制和UI系统等功能，同样适合用于工业和设备控制软件开发。对于本课题而言，Unity主要提供了以下技术支持。

其一，Unity能够直接导入球体彩灯模型，便于在编辑环境中构建与实际装置一致的三维结构。  
其二，Unity支持对单个灯具对象挂载脚本和碰撞体，使用户可通过鼠标点击完成对象选中。  
其三，Unity的光照与材质系统能够将RGBW参数变化直接映射为灯具的视觉变化，强化所见即所得的编辑体验。  
其四，Unity的Canvas与TMP组件能够构建较为灵活的参数面板、列表面板和串口控制界面。  
其五，Unity天然支持组件化脚本结构，有利于把灯具管理、场景管理、串口管理等模块分离实现。

与传统Windows窗体界面相比，Unity在异形设备可视化方面更具优势。尤其是对于球体、多边形等三维结构，Unity可以通过拖拽旋转、镜头切换和局部显隐等方式提升用户的空间认知效率。

### 3.2 彩灯分组与编号技术

根据设计图，356个彩灯被划分为31组，不同组的灯数分别为9、11、12和16。分组采用“下边界和右边界属于本组”的原则，其本质是将球体表面划分为多个具有拓扑连续性的控制区域。对于上位机而言，分组的意义至少体现在三个方面。

第一，分组是协议下发的基本单位。每组场景数据都要独立封装头部、组状态、数据块、校验和和结束位。  
第二，分组是场景组织的基本层次。一个完整场景由31组数据组成。  
第三，分组是用户局部编辑的重要视图单位。软件通过按面显示的方式减少同时可见对象数量，提高编辑效率。

除了组号外，组内编号也是协议映射的重要内容。每盏灯在本组内必须具有唯一的`lampIndex`，从而在封装时能够精确对应到数据块内的固定偏移。当前项目中，对每个彩灯都设置了组号和组内编号属性，封装阶段按`baseOffset = idx * 8`的方式写入两组RGBW数据，体现出明确的对象化设计思想。

### 3.3 RGBW参数建模技术

本课题中每个彩灯均包含两组RGBW参数，共八个字节。第一组参数用于外部显示，会直接驱动Unity中的灯光颜色和材质发光效果；第二组参数用于内部控制和协议输出，不直接显示。这一设计兼顾了“视觉编辑”与“协议编码”两个层面的需要。

在建模上，RGBW参数均限制在0到100范围内。这样做的优点在于：其一，用户更容易理解百分比式调节；其二，便于通过滑块和输入框同步编辑；其三，后续转换为字节数据时结构清晰。通过`Lamp`脚本中的`SetR`、`SetG`、`SetB`、`SetW`等方法，系统实现了参数合法性约束与显示联动。

在显示层，系统采用`Color rgb + Color.white * white`的方式对RGBW进行合成，并根据最大通道值调整光强。这种处理方式虽然不是严格的光谱混色模型，但足以在工程软件中实现直观的亮度反馈。

### 3.4 场景对象组织技术

为了支持多场景保存与切换，本文将“场景”定义为一个包含31组灯具参数的完整数据对象。项目中采用`SceneData`类表示场景，并以`Dictionary<int, List<LampData>>`形式保存各组数据。每个`LampData`记录组号、组内编号、两组RGBW数据以及摄像头状态。

这种设计具有几个优点。首先，字典结构使组数据索引清晰，便于按组遍历输出；其次，列表结构便于存放不同数量的灯具对象，适应9、11、12、16等不同组类型；再次，场景对象完全独立于当前界面状态，可以在场景列表中长期保存，并支持插入、覆盖、重载和合并。

相较于直接将所有参数以大数组形式管理，对象化场景结构更符合上位机软件的逻辑组织习惯，也更方便进行后期维护和扩展。

### 3.5 二进制协议封装技术

依据设计图文档，每组彩灯参数保存格式包括以下内容：1字节头部标志位`0xF5`、1字节组别及摄像头状态、若干字节彩灯RGBW占空比数据、2字节校验和以及1字节结束位`0xFC`。不同组的数据区长度不同，分别为72字节、88字节、96字节和128字节。

在当前项目实现中，`SceneBinSaver`使用`groupLampCounts`数组记录31组的灯数分布，并在写出时为每组构造`lampCount * 8`字节的数据块。系统通过`BuildGroupStateByte`函数将摄像头状态与组号打包进1字节，其中组号占低5位，摄像头开启状态通过中间位标识。数据区写入后，再对除头部外的数据求和并截取低8位作为校验和，最后将高4位和低4位拆成两个字节写入，避免与控制字混淆。

从协议工程角度看，上述封装方式有三个特点：一是组粒度清晰，便于下位机按组存储和解析；二是单双向控制命令统一采用标志位区分，便于状态机切换；三是通过拆分校验与参数字节，降低了控制符冲突风险。该机制为后续串口发送和存储一致性提供了保障。

### 3.6 串口通信与异步接收处理技术

当前项目通过`SerialPort`实现串口通信，并以接收线程持续监听数据。由于Unity的多数对象和UI操作只能在主线程完成，因此系统在接收线程中只负责读取数据和压入队列，而在`Update`方法中将日志输出与协议解析转移到主线程执行。这种“后台读取+主线程消费”的处理模式兼顾了通信实时性和Unity线程安全要求。

系统在串口接收时设置了两类队列：`msgQueue`用于缓存日志文本，`dataQueue`用于缓存原始数据帧。随后在主线程中调用`HandleData`，分别执行文本协议解析与`F2`反馈帧解析。为应对串口数据可能出现的粘包和分包问题，系统引入`bufferCache`作为临时缓冲区，并通过查找帧头`0xF2`和帧尾`0xFC`逐步还原完整数据包。

这种设计说明，本课题不仅关注“功能能否完成”，同时也考虑到了实际串口通信中常见的数据到达不完整问题，因此在工程实现上具有一定完整性。

## 第四章 系统详细设计与实现

### 4.1 系统模块划分

结合实际工程代码，本文将系统实现划分为以下几个核心模块：

1. 彩灯对象模块  
2. 灯具参数管理模块  
3. 面选择与显示模块  
4. 场景编辑与场景文件模块  
5. 串口通信与协议解析模块  
6. 模式切换与辅助交互模块

各模块既相互独立，又通过事件、对象引用和数据结构形成协同。

### 4.2 彩灯对象模块设计

彩灯对象模块以`Lamp.cs`为核心。每个灯具对象包含以下主要属性：`groupId`表示所属组号，`lampIndex`表示组内编号，`hasCamera`表示是否具备摄像头属性，`cameraOn`表示当前摄像头状态；同时还包含外圈RGBW参数和内圈RGBW参数共八个数据项。

在功能设计上，`Lamp`脚本主要承担四类职责。  
第一类是数据保存职责，即对灯具当前所有控制参数进行封装和持有。  
第二类是显示刷新职责，当外圈RGBW参数变化时，通过`ApplyLight()`更新灯光颜色和材质发光效果。  
第三类是交互触发职责，在鼠标点击时通知`LampManager`进行选中处理。  
第四类是状态清零职责，通过`ResetAllData()`完成参数复位。

从软件设计角度看，将灯具抽象为独立对象，能够使三维场景中的每一盏灯都成为可交互、可存储、可编码的最小控制单元，这是本系统成功实现可视化编辑的基础。

### 4.3 灯具参数管理模块设计

灯具参数管理由`LampManager.cs`完成。该模块负责当前选中灯具的维护，以及参数面板与灯具对象之间的双向同步。系统中既有滑块控件，也有输入框控件。用户通过滑块改变值时，系统将实时更新当前灯具属性；用户直接输入数值时，系统通过解析文本内容反向设置滑块值，继而驱动灯具参数变更。

为了避免参数修改只停留在界面而未能及时写回数据对象，系统提供了`CommitCurrentLamp()`方法，在关闭面板、切换面、重置、生成文件等关键操作前先提交当前灯具状态。与此同时，模块通过`OnLampValuesChanged`事件将参数变化通知给场景管理模块，使“当前灯具状态”与“当前场景缓存”保持同步。

在用户体验方面，`LampManager`还负责灯具高亮、取消选中、摄像头开关面板显隐和整体参数重置。尤其是对“部分灯具存在摄像头、部分灯具不存在摄像头”的实际情况，系统通过`hasCamera`字段控制Toggle显示逻辑，使界面行为与物理属性保持一致。

### 4.4 面选择与显隐控制模块设计

球体彩灯系统若将所有灯具同时显示，会导致界面过于拥挤，不利于局部编辑。因此项目中引入“面”的概念，并通过`FaceManager`、`FaceLightManager`和`FaceUIController`三部分协同实现。

`FaceManager`主要负责视角方向控制。当用户在界面中选择某一面时，系统将调用`FaceRotator.RotateToFaceDir()`，根据目标面的法向量与摄像机朝向关系自动旋转球体，使目标面朝向用户视角。  
`FaceLightManager`主要负责灯具的显示与隐藏。切换到某一面时，系统先关闭参数面板，再隐藏所有面上的灯具，仅显示当前选择面的灯具对象。  
`FaceUIController`作为界面入口，将下拉选择结果同时转发给视角控制模块和显隐控制模块。

这种设计使“看向哪一面”和“编辑哪一面”保持一致，大幅降低了用户在复杂结构中定位灯具的难度。

### 4.5 模型拖拽与视角恢复设计

除按面自动旋转外，系统还提供了自由观察功能，由`DragRotate.cs`完成。该脚本区分左键水平旋转与右键垂直旋转，并设置最小和最大俯仰角，防止模型翻转过度影响观察。为了提升使用便捷性，脚本还实现了双击空白处恢复默认视角的功能，用户在长时间编辑后可快速回到初始观察状态。

在实现细节上，该脚本能够识别当前是否点击到UI控件，避免在操作输入框和滑块时误触发模型旋转；同时还会判断当前是否处于串口模式，禁止在不合适的模式下触发双击复位逻辑。这说明系统在交互设计上考虑了模式隔离与误操作防护。

### 4.6 场景编辑与管理模块设计

场景编辑与管理是本课题的核心功能之一，主要由`SceneUIController.cs`和`SceneBinSaver.cs`协同实现。

#### 4.6.1 场景采集与新增

`SceneBinSaver`提供`CaptureCurrentScene()`和`AddCurrentScene()`等方法，用于读取当前场景中所有灯具对象的状态并封装为`SceneData`。采集时系统会遍历所有灯具，将每盏灯的组号、编号、RGBW参数和摄像头状态写入相应组列表中，然后加入场景集合。

#### 4.6.2 场景插入与顺序维护

除简单追加外，系统还支持按用户输入的序号将当前场景插入到指定位置。`SceneUIController`中通过输入框获取一基序号，并完成合法范围检查，随后调用`InsertScene()`实现插入。由于实际使用中场景顺序往往对应显示顺序，这一功能具有较强实用价值。

#### 4.6.3 下拉切换与编辑模式

项目中的场景下拉框不仅用于浏览场景列表，还用于场景应用和场景覆盖式编辑。当用户处于普通模式时，切换下拉选项会将对应场景应用到当前灯具模型中；当用户处于“编辑当前下拉场景”模式时，用户对灯具参数的修改会直接回写到当前选中的场景对象中，从而实现对已有场景的精修。为了防止编辑过程中执行不安全操作，系统还对生成文件、加载文件、切换模式等动作加入了阻断提示。

#### 4.6.4 场景合并与覆盖加载

考虑到用户可能需要在已有项目基础上继续编辑，系统支持从外部BIN文件加载全部场景，也支持将外部BIN文件中的场景插入到当前场景列表的前部或后部。此功能通过`SceneBinLoader.TryParseBin()`解析外部文件，再由`SceneUIController`决定插入方式。相较于单纯覆盖加载，场景合并能力显著提高了软件的复用性。

### 4.7 二进制文件生成模块设计

二进制文件生成由`SceneBinSaver`承担。系统在保存文件时，先提示用户选择输出路径，然后依次遍历场景列表中的每个场景，再在每个场景内部按1到31组的顺序写入各组数据。

每组写入过程包括以下步骤：

1. 写入头部字节`0xF5`。  
2. 统计该组摄像头状态并构造组状态字节。  
3. 根据组灯数创建数据块，并按`lampIndex * 8`偏移写入两组RGBW参数。  
4. 对组状态字节与数据区做累加和校验，拆成两个4位字节写入。  
5. 写入结束字节`0xFC`。

为增强可用性，系统在生成前会检测当前是否存在尚未通过“添加场景”写入列表的灯具修改，并通过确认对话框提醒用户是否需要先保存当前场景。该机制能有效减少因为遗忘保存而造成的数据丢失问题。

### 4.8 二进制文件解析模块设计

与文件生成对应，`SceneBinLoader`实现了`.bin`文件的读取与解析。其核心思想是按协议逆向还原场景对象。系统从文件头开始，依次按31组解析一帧完整场景：首先检查头部是否为`0xF5`，然后读取组状态字节、数据区、校验区和结束位。解析完成31组后，构成一个完整场景对象并加入场景列表，直到文件结束。

这种设计实现了“上位机自己生成的数据能被自己重新读取”的闭环，也为文件合并、历史工程复用和错误恢复提供了基础。

### 4.9 串口通信模块设计

串口相关功能主要由`SerialManager.cs`和`SerialUIController.cs`实现。

`SerialUIController`面向界面交互，负责端口列表刷新、波特率初始化、串口开关按钮逻辑、BIN文件选择、BIN发送以及运行、清空和检测命令按钮响应。  
`SerialManager`面向底层通信，负责串口打开与关闭、发送字节数组、接收线程管理、日志入队、原始数据入队、缓存解析以及反馈消息输出。

在实现上，系统预设常用波特率，并允许用户从下拉框中选择端口和波特率后打开串口。若串口已打开，则按钮功能切换为关闭串口。对BIN文件发送操作，系统要求串口已连接且文件已选择，防止无效发送。对于运行命令，系统按协议将循环间隔拆成两字节、场景数拆成四字节、循环次数拆成两字节，最终封装为10字节的`F8`命令帧。

### 4.10 线路检查反馈解析设计

在线路检查场景下，上位机发送`F7 00 00 FC`后，下位机可能返回以`F2`开头、以`FC`结尾的反馈帧。当前项目实现中，`SerialManager.TryParseBuffer()`持续在缓存中寻找长度为7字节的`F2`帧，并交由`ParseLineCheck()`解析。

解析逻辑会先读取组号，再将四个4位数据组合为16位状态值。随后根据组号对应的实际灯数，逐个判断各从机灯具是否正常。系统采用二进制状态位判断是否在线，当某一位为0时，即在日志中输出“某组某号灯异常”。这种实现方式与设计图文档中的线路检查反馈方案保持一致，能够满足联调阶段的故障提示需求。

### 4.11 模式切换与辅助模块设计

系统提供编辑模式和串口模式两种工作状态，由`UIStateController.cs`控制。在编辑模式下，重点显示场景编辑面板和球体观察界面；在串口模式下，系统通过摄像机位移、面板动画和球体自动旋转切换至串口操作视图。切换模式前，系统会调用`ResetController.ResetAll()`完成视角复位、显示全部面及关闭灯具控制面板，保证界面状态整洁。

此外，`UILogger.cs`用于统一显示串口日志和解析结果，并在文本长度过大时截断旧内容；`CommitHint.cs`用于在关键时刻提醒用户当前灯具编辑状态已提交。这些辅助模块虽然不直接承担核心算法，但对整体使用体验具有明显促进作用。

## 第五章 系统测试与结果分析

### 5.1 测试目标

系统测试的主要目标是验证软件是否满足开题报告和设计图中提出的功能要求，重点包括：

1. 三维模型是否能够正常显示与旋转。  
2. 面选择与灯具显隐是否正确。  
3. 单灯参数编辑与界面联动是否正确。  
4. 场景新增、插入、切换、合并与覆盖是否正确。  
5. BIN文件生成与读取是否满足协议要求。  
6. 串口命令发送与反馈解析是否符合设计目标。  
7. 在常见用户操作流程下，系统是否具备较好的稳定性与可用性。

### 5.2 测试环境

系统基于Unity与C#开发，运行环境为Windows桌面平台。测试对象主要包括Unity工程中的三维界面模块、场景管理模块与串口通信模块。由于本课题属于软硬件协同设计，测试既包括纯软件层的功能验证，也包括围绕串口命令与协议格式的联调验证。

### 5.3 三维交互功能测试

在三维交互测试中，首先启动程序并加载球体彩灯模型，观察模型是否能够完整显示。随后使用鼠标左键拖拽执行水平方向旋转，使用鼠标右键拖拽执行俯仰旋转，并通过双击空白区域测试视角复位功能。测试结果表明，模型可根据输入进行平稳旋转，俯仰角限制生效，双击后可恢复至初始观察状态，说明拖拽旋转与视角复位逻辑能够正常工作。

接着对“面选择”功能进行测试。在选择不同面后，系统能够自动调整球体朝向，并仅显示目标面相关灯具。切换过程中原先打开的灯具控制面板被关闭，避免了用户在错误上下文中继续编辑先前灯具。该结果说明系统已实现视角与显隐联动，满足局部编辑需求。

### 5.4 单灯参数编辑测试

单灯参数编辑测试主要验证选中、高亮、参数同步和实时显示效果。测试时先点击任意灯具，观察是否出现高亮效果以及参数面板是否成功弹出。随后分别调节外圈RGBW滑块和输入框，观察灯光颜色、材质发光与输入值是否同步变化；再调节内圈RGBW参数，确认其虽然不影响当前可视效果，但能保留在灯具对象中。

测试结果表明，用户点击灯具后系统能够正确记录当前选中对象，面板显示内容与灯具原始参数一致；外圈参数变化时，灯光显示会立即更新；输入框与滑块之间可以双向同步；切换至其他灯具时，上一个灯具高亮被取消，当前灯具高亮被激活。对于具备摄像头属性的灯具，摄像头开关控件能够正常显示并写入状态；对于不具备该属性的灯具，相关控件被隐藏，说明逻辑约束有效。

### 5.5 场景管理功能测试

围绕场景管理，测试内容包括新增场景、指定位置插入场景、下拉应用场景、编辑已有场景、从BIN加载场景以及与当前列表合并等。

首先，在设置若干灯具参数后执行“添加场景”，系统能够将当前全部356个灯具的状态保存为一个场景对象，并在下拉框中新增选项。随后修改部分灯具状态后再添加第二个场景，切换下拉框可观察到不同场景在模型上正确切换。说明场景采集和应用逻辑有效。

其次，通过输入目标序号执行场景插入操作，测试结果显示系统可将当前场景插入指定位置，并自动刷新下拉列表。若输入为空、非数字或超出范围，界面会给出提示信息，说明系统具备基本输入合法性校验能力。

再次，进入“编辑当前场景”模式后，对当前场景中的灯具参数进行修改，切换到其他场景再返回，修改内容仍然保留，表明场景回写逻辑正确。系统在编辑模式下会阻止部分危险操作，能够降低数据丢失风险。

最后，对BIN加载与合并功能进行测试。加载已有BIN文件后，系统可重新构建场景列表；将外部BIN合并到当前列表前部或后部后，场景总数与下拉内容均正确变化。说明场景解析、追加和插入机制较为完善。

### 5.6 二进制文件协议测试

二进制文件测试主要验证生成内容是否满足协议要求，以及解析逻辑是否能恢复原始场景。

在文件生成测试中，先构造包含多个场景的工程数据，再执行保存操作。系统能够弹出保存窗口并生成`.bin`文件。根据代码实现分析，每个场景由31组数据连续组成，每组均按照`F5 + 组状态 + 数据区 + 校验和 + FC`格式输出，且数据区长度随组灯数变化而变化。由于场景采集与导出都基于同一组号、编号与参数结构，因此文件内部组织与设计图要求是一致的。

在回读测试中，将生成的BIN文件重新载入系统，解析出的场景数量与原始输出数量保持一致，各场景切换后灯具状态恢复正确，说明文件输出和文件解析形成了闭环。对工程应用而言，这意味着用户可将历史灯光方案保存、复用和再次编辑。

### 5.7 串口命令与反馈测试

串口功能测试分为端口连接测试、BIN发送测试、运行命令测试、清空命令测试和线路检查反馈测试五部分。

在端口连接测试中，程序启动后可列出当前可用串口，用户选择端口和波特率后可打开串口，再次点击按钮则关闭串口，状态切换符合预期。  
在BIN发送测试中，若未选择文件或串口未打开，系统会给出错误提示；满足条件后发送文件，日志区显示发送开始与完成信息。  
在运行命令测试中，用户输入循环间隔、场景数和循环次数后，系统能够封装并发送`F8`命令帧。若下位机返回形如`RUN_`的反馈字符串，日志区会自动显示运行参数反馈内容。  
在清空命令测试中，系统发送`F6 00 00 FC`后，若收到`F6 OK`文本，则自动记录“清除成功”提示。  
在线路检查测试中，系统发送`F7 00 00 FC`，当接收到`F2`帧后能够识别组号并解析状态位，对异常灯具给出日志提示。

测试结果表明，上位机在串口通信层面已形成较完整的控制链路，具备设备联调所需的基础能力。

### 5.8 软件特性分析

通过上述测试可以看出，本文设计的系统具有以下几个较为明显的特点。

第一，交互过程贴近真实设备结构。与传统二维表格配置方式相比，三维球体观察、面选择与灯具点选的组合使用户能够更直观地理解彩灯空间分布。  
第二，数据结构清晰。系统以灯具对象和场景对象为核心，实现了编辑态、缓存态和文件态之间的一致转换。  
第三，协议适配性较强。无论是场景数据帧、运行命令还是线路检查反馈，系统都围绕下位机协议进行了明确实现。  
第四，工程实用性较好。场景插入、合并、覆盖加载和日志反馈等功能说明该软件不仅适合“演示”，也适合实际调试和配置工作。

当然，系统也仍存在进一步优化空间。例如当前日志反馈仍以文本为主，未来可增加更直观的故障面板；当前串口协议以固定格式为主，未来可扩展更多设备配置命令；当前场景列表更多是基础管理，未来还可增加拖拽排序、批量复制和预览功能。

## 第六章 总结与展望

### 6.1 工作总结

本文围绕“基于编码彩灯的上位机APP设计”这一课题，结合开题报告、系统设计图及当前Unity工程实现内容，完成了一套面向球形编码彩灯阵列的上位机软件方案研究与实现分析。全文从需求出发，围绕三维可视化交互、彩灯对象建模、场景组织管理、二进制文件封装以及串口通信联调等关键问题进行了系统阐述。

在系统设计方面，本文首先分析了356个彩灯、31个控制组的硬件背景与控制需求，明确了软件应同时具备可视化编辑、场景管理、协议生成和命令发送等功能。随后，基于Unity和C#提出总体架构方案，将系统划分为用户交互层、业务控制层、数据模型层、文件输出层和通信交互层，并围绕这一架构完成了模块化设计。

在关键技术方面，本文研究了球体彩灯的分组与编号方法、两组RGBW参数的对象化建模方式、按组协议封装的二进制输出结构以及基于接收缓存的串口反馈解析方法。针对异形彩灯控制中“结构复杂、参数量大、协议固定、反馈链路多样”的特点，本文通过三维界面与对象模型结合的方式，提升了编辑直观性与数据一致性。

在实现层面，本文结合实际工程脚本，对`Lamp`、`LampManager`、`SceneBinSaver`、`SceneBinLoader`、`SceneUIController`、`SerialManager`、`SerialUIController`等模块进行了说明，展示了系统如何从单灯参数编辑扩展到多场景管理，再扩展到串口发送与线路检查反馈。研究结果表明，该系统已经能够较完整地支撑编码彩灯阵列的配置、存储、传输和联调流程。

在参考方法上，本文借鉴了用户提供的两篇上位机相关文献中的模块化设计和测试验证思路。前者强调以C#构建专用设备上位机软件，后者强调上位机软件的设计与测试并重。这些思路与本课题的工程特性相契合，也进一步说明上位机软件开发必须面向具体对象和具体协议进行定制化设计。

综合来看，本文完成的主要工作可以概括为以下几点：

1. 完成了面向31组356盏彩灯的三维可视化上位机设计思路梳理。  
2. 建立了基于灯具对象和场景对象的数据组织结构。  
3. 实现了多场景二进制文件生成、读取与合并方案。  
4. 实现了串口连接、文件发送、运行命令、清除命令和线路检查命令功能。  
5. 形成了较完整的功能测试与结果分析框架。  

### 6.2 不足之处

尽管本系统已经具备较完整的功能，但从进一步工程化角度看，仍存在以下不足。

其一，当前系统主要围绕本课题对应的特定硬件协议展开设计，协议可配置性还不够强。若未来硬件协议发生较大变化，部分编码逻辑需要手动修改。  
其二，当前线路检查反馈主要以日志文本方式展示，缺少更图形化的异常定位界面。若系统规模继续扩大，文本方式的可读性会下降。  
其三，场景管理仍以基础列表为主，尚未加入场景缩略预览、批量复制、批量删除或拖拽排序等更高级功能。  
其四，当前串口通信主要面向本地有线调试，尚未扩展至网络通信、远程协同或跨端控制。  
其五，系统测试部分已覆盖主要功能流程，但若作为正式工程产品，仍需在更多硬件工况下进行长期稳定性验证。

### 6.3 未来展望

结合当前研究成果，系统后续可从以下几个方向继续改进。

第一，增强协议配置能力。可将组数、灯数分布、命令字与校验规则抽离为配置文件，实现更高程度的协议适配与复用。  
第二，完善可视化诊断功能。可将线路检查结果直接叠加显示在三维模型或二维分组示意图上，以颜色和标记方式快速定位异常灯具。  
第三，扩展场景编辑能力。可加入场景复制、模板复用、动画预览、场景批量导出等功能，提升软件在真实演出和景观控制中的实用性。  
第四，拓展通信方式。可在串口基础上增加TCP/IP、无线网络或蓝牙等通信接口，为远程调试和多终端控制提供支持。  
第五，结合数字孪生方向发展。未来可将下位机实时状态、运行日志和故障信息持续映射到三维模型中，进一步提升软件的监控属性，而不仅仅是参数配置属性。

总体而言，本文围绕一个具体而真实的工程课题，完成了从需求分析到方案设计、从核心实现到测试验证的系统性研究。基于编码彩灯的上位机APP不仅解决了当前项目中的实际问题，也为后续类似异形灯阵控制系统的软件开发提供了可借鉴的技术路线。

## 参考文献

[1] 卓树峰. 基于STM32的舞台灯光控制系统设计[J]. 齐齐哈尔大学学报, 2016, 32(6): 5-9.  
[2] 陈丹, 柯熙政, 王佳. 一种智能调光系统的研制与实现[J]. 西安理工大学学报, 2013, 29(1): 45-49.  
[3] 王业罡. 基于Art-Net协议的嵌入式调光系统的研究与设计[D]. 延边大学, 2007.  
[4] 钱雨, 张传武, 范旭杰. 基于STM32的RDM灯具控制系统[J]. 软件工程与应用, 2020, 9(6): 447-455.  
[5] Zhang Y J, Yin X N. Design of Remote Control System for Smart Home Based on Unity and the Internet of Thing[J]. Journal of Physics: Conference Series, 2021, 1744(2): 022099.  
[6] Ilchev S. Design and Implementation of Firmware for an Embedded System that Creates Lighting and Laser Effects[C]// Proc. of the Int. Conf. on Computer Systems and Technologies. ACM, 2023: 9-14.  
[7] ESTA. ANSI E1.11-2008(R2018), USITT DMX512-A Standard[S]. New York: Entertainment Services and Technology Association, 2018.  
[8] Trejo-Macotela F R, García-Barrientos A, Bautista-Flores A, et al. Controlador Digital Mediante el Protocolo DMX-512[C]// 2013 Simposio Iberoamericano Multidisciplinario de Ciencias e Ingenierias. 2013.  
[9] Nguyen M H, Lee C H. Design of Bi-directional RDM-DMX512 Converter for LED Lighting Control[J]. International Journal of Fuzzy Logic and Intelligent Systems, 2013, 13(2): 106-115.  
[10] Jung H J, Lee W B. A Study on the Implementation of Smart Farm Environment Control System Using Unity and Photon[J]. Journal of the Semiconductor & Display Technology, 2021, 20(1): 104-107.  
[11] 李从宏, 陈杨渊. 基于C#的土壤渗透系数测量系统上位机软件设计[J]. 电脑编程技巧与维护, 2024(11): 40-42.  
[12] 申漫漫. 光伏发电系统上位机软件设计与测试[J]. 光源与照明, 2025(08): 147-149.  

## 附录A 主要协议格式说明

### A.1 场景数据帧

每个场景由31组数据组成，每组数据采用如下结构：

`F5 + 组别及摄像头状态 + 数据区 + 校验和两字节 + FC`

其中数据区长度由当前组彩灯数量决定。每个彩灯占8字节，分别对应外圈RGBW和内圈RGBW。

### A.2 运行命令帧

运行命令采用如下格式：

`F8 + 循环间隔两字节 + 场景数四字节 + 循环次数两字节 + FC`

系统中对较大数值采用拆分表示方式，以避免控制字符混淆。

### A.3 清空命令帧

清空命令格式为：

`F6 00 00 FC`

若下位机执行成功，返回文本反馈`F6 OK`。

### A.4 线路检查命令帧

线路检查命令格式为：

`F7 00 00 FC`

主下位机回传格式可抽象为：

`F2 + 组地址 + 在线状态位 + FC`

系统通过重新组合状态位并结合组灯数判断异常灯具。

## 附录B 主要功能文件说明

为便于后续毕业答辩展示与论文修改，现对当前项目中的主要脚本作用进行归纳：

1. `Lamp.cs`：彩灯对象定义与显示更新。  
2. `LampManager.cs`：灯具选中、参数面板联动与参数提交。  
3. `SceneBinSaver.cs`：场景采集、场景导出和二进制封装。  
4. `SceneBinLoader.cs`：二进制解析和场景加载。  
5. `SceneUIController.cs`：场景下拉框、插入、编辑模式和合并逻辑。  
6. `SerialManager.cs`：串口底层收发与反馈解析。  
7. `SerialUIController.cs`：串口界面按钮与命令发送。  
8. `FaceManager.cs`、`FaceLightManager.cs`、`FaceUIController.cs`：面选择、视角切换与局部显隐。  
9. `DragRotate.cs`：球体拖拽观察与双击复位。  
10. `UIStateController.cs`：编辑模式与串口模式切换。  

上述脚本共同构成了本课题的软件实现基础，也与正文中各模块设计内容一一对应。
